name: Keep Render Alive

on:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Personal
        run: curl -s https://beauty-crm-backend-personal.onrender.com/api/members
      - name: Ping Customer
        run: curl -s https://beauty-crm-backend-customer.onrender.com/api/members